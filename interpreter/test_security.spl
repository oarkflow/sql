// Allowed command
puts(">> Testing allowed command (echo):")
let out1 = exec("echo", "Hello Secure World")
puts(out1)

// Disallowed command
puts(">> Testing disallowed command (rm):")
let out2 = exec("rm", "/tmp/doesnotexist")
puts(out2)

// Another disallowed command
puts(">> Testing disallowed command (id):")
let out3 = exec("id")
puts(out3)
